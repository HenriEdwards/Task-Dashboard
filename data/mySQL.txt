CREATE TABLE users (
	user_ID int AUTO_INCREMENT,
    username varchar(255),
    password varchar (255),
    PRIMARY KEY (user_ID)
)

CREATE TABLE status (
	status_ID int AUTO_INCREMENT,
    status varchar(255),
    PRIMARY KEY (status_ID)
)

CREATE TABLE tasks (
	task_ID int AUTO_INCREMENT,
    title varchar(255),
    description varchar(255),
	due_date DATE,
    status_ID int,
    user_ID int,
    PRIMARY KEY (task_ID),
    FOREIGN KEY (status_ID) REFERENCES status(status_ID),
    FOREIGN KEY (user_ID) REFERENCES users(user_ID)
)

INSERT INTO users (username, password) VALUES
  ('jack', 'jack'),
  ('jane_smith', 'abc123'),
  ('michael_johnson', 'letmein');

INSERT INTO status (status) VALUES
  ('Pending'),
  ('In Progress'),
  ('Completed');

INSERT INTO tasks (title, description, due_date, status_ID, user_ID) VALUES
  ('Finish project proposal', 'Write up the project proposal document', '2023-08-15', 1, 1),
  ('Implement new feature', 'Develop the new user authentication feature', '2023-09-01', 2, 2),
  ('Review marketing materials', 'Proofread and provide feedback on the new brochure', '2023-07-31', 3, 1),
  ('Attend team meeting', 'Discuss project status and next steps', '2023-08-10', 2, 3),
  ('Prepare quarterly report', 'Gather data and create the Q3 report', '2023-10-01', 1, 2);